{
  "extends": ["../../.eslintrc.json"],
  "ignorePatterns": ["!**/*"],
  "overrides": [
    {
      "files": ["**.test.ts"],
      "env": { "jest": false },
      "rules": { "max-nested-callbacks": "off" }
    }
  ]
}
