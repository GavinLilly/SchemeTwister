import { SchemeDirective } from './scheme.directive';

describe('SchemeDirective', () => {
  it('should create an instance', () => {
    const directive = new SchemeDirective();
    expect(directive).toBeTruthy();
  });
});
