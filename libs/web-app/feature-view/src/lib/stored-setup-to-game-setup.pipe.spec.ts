import { StoredSetupToGameSetupPipe } from './stored-setup-to-game-setup.pipe';

describe('StoredSetupToGameSetupPipe', () => {
  it('create an instance', () => {
    const pipe = new StoredSetupToGameSetupPipe();
    expect(pipe).toBeTruthy();
  });
});
