import { LatestSetupsStoreStore } from './latest-setups-store.store';

describe('LatestSetupsStoreStore', () => {
  const componentStore = new LatestSetupsStoreStore();

  it('should be created', () => {
    expect(componentStore).toBeTruthy();
  });
});
