import { CardAsHeroPipe } from './card-as-hero.pipe';

describe('CardAsHeroPipe', () => {
  it('create an instance', () => {
    const pipe = new CardAsHeroPipe();
    expect(pipe).toBeTruthy();
  });
});
