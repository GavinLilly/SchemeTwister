# libtwister-testing-data

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build libtwister-testing-data` to build the library.

## Running unit tests

Run `nx test libtwister-testing-data` to execute the unit tests via [Vitest](https://vitest.dev/).
