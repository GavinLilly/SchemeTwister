import { Route } from '@angular/router';
import { RandomizeComponent } from './randomize/randomize.component';

export const randomizeRoutes: Route[] = [
  { path: '', component: RandomizeComponent },
];
