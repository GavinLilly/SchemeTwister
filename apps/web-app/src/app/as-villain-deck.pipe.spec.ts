import { AsVillainDeckPipe } from './as-villain-deck.pipe';

describe('AsVillainDeckPipe', () => {
  it('create an instance', () => {
    const pipe = new AsVillainDeckPipe();
    expect(pipe).toBeTruthy();
  });
});
